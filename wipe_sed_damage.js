const fs = require('fs');

// I am going to fetch the file from git history again, properly this time.
// Since it's not a git repository, I will just manually fix the syntax errors. The problem is I deleted backticks and curly braces inside the template string.
